#include <stdio.h>
/**
 * main - printing single digit numbers based on 10 starting from 0
 * Return:Always 0
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	putchar('\n');
	return (0);
}
