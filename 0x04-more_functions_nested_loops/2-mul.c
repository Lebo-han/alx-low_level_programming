#include "main.h"
/**
 * mul - multiplies two integers
 * @x: the first integer to be multiplied
 * @y: the second integer to be multiplied with the first integer
 * Return: the multiplication result between x and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
